import React from 'react';
import styles from '/styles/Settings.module.css'; // Ensure the path is correct

function Settings(props) {
    return (
        <div className={styles.settings}>
            <h1>Settings Page</h1>
        </div>
    );
}

export default Settings;
